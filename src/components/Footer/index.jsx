import React from 'react';
import PropTypes from 'prop-types';
import './style.scss';
Footer.propTypes = {
    
};

function Footer(props) {
    return (
        <div>
            <p>Footer</p>
        </div>
    );
}

export default Footer;