//dung lenh rsfp
import React from 'react';
import PropTypes from 'prop-types';
import './style.scss'; //import file scss cua loading
Loading.propTypes = {
    
};

function Loading(props) {
    return (
        <div>
            <p>Loading</p>
        </div>
    );
}

export default Loading;